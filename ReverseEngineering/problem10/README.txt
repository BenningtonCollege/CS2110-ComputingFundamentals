I've started to notice that some features in Processing render differently in
the web browser as an applet versus running within the Processing IDE or
as a standalone application, so I've exported to the application format, too.